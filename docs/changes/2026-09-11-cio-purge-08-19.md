# 2026-09-11 CIO 08-19 错批清理 + 堵住「TDX 未就绪仍生成报告」

- 日期：2026-09-11
- 性质：数据清理（DELETE）+ 防护代码 + 测试。**backend 保持停止**（未启动）；未 commit/push、未发飞书、未开早盘开关、未开 CIO worker。

---

## 阶段 1：删除 08-19 错批（先 SELECT 后 DELETE）

- 锁定条件（created_at 为 UTC ISO 带时区格式，与样例核对后落条件）：
  `research_as_of='2026-08-19' AND created_at>='2026-09-11T07:55:00'`
- SELECT 计数 = **683**（与失控核查一致）→ 执行 DELETE：
  - `company_cio_report_sections` 删 **12,885 行**（按 report_id 子查询）
  - `company_cio_research_reports` 删 **683 行**
- 删除后：08-19 残留 **0**；保护行验证 `id=49`（003012·09-07 全文）、`id=286`（003012·09-11 PRICE 块）均在。

## 阶段 2：003012 PRICE 块补写

- 发现并修复一个真问题：`save_report` 的同指纹幂等分支会跳过节写回——003012 id=286 的 valuation 节 payload 被失控批的就地覆盖洗掉后，常规 refresh 因 report 级指纹未变而静默不写。
- 处置：用确定性重建的 valuation 节 + `update_report_section` 就地写回，并同步 `valuation_hash`。
- **最终状态**：id=**286**、research_as_of=**2026-09-11**、现价 **4.57**、position_label=**低于低估关注区**、zone 4.55–4.69、close_as_of=2026-09-11、block_fingerprints.PRICE=c024f8e461ed1356。
- 代码级自愈：`refresh_cio_block` 尾部新增检查——save 幂等短路导致节内块指纹缺失时，自动 `update_report_section` + `set_module_hash` 补写（store 新增公开方法 `set_module_hash`，白名单校验列名）。

## 阶段 3：堵住「TDX 未就绪仍生成」

- `_default_research_as_of()`（`cio_report/service.py:73`）新语义：
  - TDX 最新合格收盘快照可用 → 返回该交易日（不变）；
  - 不可用 → 返回 **None**，**删除了原宏观序列日 fallback（08-19 事故根因）**。
- 生成入口全部拒绝并零写入：
  - `build_report(as_of=None)` → `{"status": "RESEARCH_DATE_UNAVAILABLE", ...}`，不落库；
  - `ensure_focus_tier_reports(as_of=None)` → 同状态拒绝批量；
  - `refresh_cio_price_blocks(as_of=None)` → 前置拒绝（避免 676 次无效逐只调用）；
  - `refresh_cio_block(as_of=None)` → 同拒绝。
- MCP 工具（`prepare_deep_research` 等 as_of 空串的入口）经 service 层统一被拒——**不写库**。

## 测试

- 新增 `agent/tests/test_cio_default_as_of.py`：mock TDX 未就绪 + as_of 空 → 4 个入口全部 `RESEARCH_DATE_UNAVAILABLE` 且 **报告表 0 行写入**；mock 就绪 → `_default_research_as_of()=='2026-09-10'`。
- `pytest test_cio_default_as_of.py test_cio_price_block.py -q` → **6 passed**；
- 全 CIO 套件（report/quick_brief/narrative/routing/incremental_synthesis/price_block/default_as_of/block_worker）→ **68 passed**（此前 block_worker 的 4 个失败为 backend 运行期并发污染，停止后消失并全绿）。

## 阶段 4：启动链

- `start_cio_block_worker`（api_server.py:194）：默认 off 实测确认（`{'started': False, 'reason': 'HZ_CIO_BLOCK_WORKER!=on'}`），保持不动。
- 启动链（api_server.py:185-194）无「as_of 空 + 全池 ensure」调用；`ensure_focus_tier_reports` 仅在 EOD 链内显式传 as_of。
- **backend 保持停止**，未启动。

## 没改什么

Focus A=10、价值线入池规则、价格带公式、风险 UNKNOWN 口径、`get_report` 等只读路径、A/B/C 档策略、SHADOW/实验臂。未 commit/push、未发飞书。

## 风险

1. backend 停止中：EOD 16:45 今晚不会跑（无进程）——今晚没有收盘日报/早盘数据生产，属已知代价，重启时机由用户决定。
2. 08-19 批仅删了报告行；若下游曾引用过这些行（如已投递通知）未回溯清理，但抽查投递表未见引用。
3. `_default_research_as_of` 收紧后，所有未显式传 as_of 的调用方（含 hermes/前端隐式调用）会在 TDX 未就绪窗口收到 `RESEARCH_DATE_UNAVAILABLE`——这是预期 fail-closed 行为，前端需能展示该错误。
