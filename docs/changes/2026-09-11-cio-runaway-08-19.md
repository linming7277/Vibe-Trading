# 2026-09-11 CIO 失控批量写入（research_as_of=2026-08-19）：停止与挂点核查

- 日期：2026-09-11
- 性质：阶段 A 停写（已执行）+ 阶段 B 只读查因。**未删任何数据行**、未改代码、未 commit、未发飞书、未开早盘开关、未再启动 backend。

---

## 阶段 A：停写（完成）

1. 停前状态：backend PID **39076**（8899 端口），启动 16:07:34，health healthy。
2. 用仓库既有方式停止：`launcher/windows/HengzhiLauncher.ps1 -Action stop`（连续执行两次确认）→ backend/frontend 均 `stopped`（Pid 0），**8899 无监听、PID 39076 已消失**。
3. 停止后未再启动。
4. 残留进程清点：命中的 python 进程里，hzstock 侧仅剩 3 个 `mcp_server.py`（hermes 会话用）与 hermes gateway——**无任何 CIO / ensure_focus / refresh_cio / synthesize 进程**。唯一 uvicorn 残留（PID 30784/41884，端口 8000）属于**另一项目 `D:\AI\money-system`**，与本系统无关，未动。

**写入是否还在涨：否。** 全表最新 `created_at` 停在 **2026-09-11T08:08:18 UTC（北京 16:08:18）**，停止核查（16:1x–16:2x）期间零增长。实际批量窗口仅 **54 秒**（16:07:24–16:08:18），是「启动即跑完」型，不是持续后台——stop 的作用是确保它不再被任何重启/调度唤醒。

## 阶段 B：挂点核查

### 1) api_server 启动链（`agent/api_server.py`）

| 行 | 调用 | 状态 |
|---|---|---|
| 185 | `start_data_refresh_scheduler()` | 启动即刷 TDX 数据（不写 CIO） |
| 186 | `start_value_research_scheduler()`（automation.py:672，`_loop` 启动即 `tick`） | 16:07 未到 16:45 且 last_status=completed → 空转 |
| 190 | `start_morning_macro_scheduler()` | HZ_MORNING_MACRO 未设 → 无线程 |
| **194** | **`start_cio_block_worker()`（并行会话新增）** | 闸正确：`HZ_CIO_BLOCK_WORKER != on` → **未启动**（block_worker.py:189-193） |

EOD 链内另有三个 CIO 挂点（均只在 16:45 run 里执行，与本次窗口不符）：
- `automation.py:418-420` `enqueue_eod_block_jobs(as_of)`——**`cio_block_jobs` 表实测 0 行**，从未入队；
- `automation.py:581` `refresh_cio_price_blocks(as_of)`（本会话所挂）；
- `automation.py:570` `ensure_focus_tier_reports(as_of)`（显式传 as_of，不会取 08-19）。

### 2) 谁在启动后得到 research_as_of=2026-08-19

- 确定性入口：`cio_report/service.py:73` `_default_research_as_of()`——优先取 TDX「最新合格收盘快照」；**进程刚启动、TDX 桥未就绪时** fallback 到 `research_freshness._resolve_as_of(None)`，本次返回 **2026-08-19**（宏观 breadth/risk_appetite 序列的最新观察日）。
- 走该默认值的调用面：**MCP 工具 `prepare_deep_research` / CIO 读取-构建类工具**（`mcp_server.py:890` / `:839` 起），as_of 参数为空串即落入默认。
- 结论（证据收敛）：683 行写入窗口（16:07:24–16:08:18，54 秒、~13 行/秒）与 backend 重启瞬间重合、backend 应用日志与 HTTP 日志均无 POST/CIO 记录、EOD 链与 block_worker 均未运行 → 调用方为 **MCP 工具面的机器批量调用**（hermes/并行 agent 会话逐只触发 prepare/构建，as_of 为空 → 落 08-19）。精确到会话 ID 需要日志改造才能定罪（现状无应用级调用日志）。
- **风险重述**：08-19 是错误的基准日（当天并非交易/研究日数据就绪日）；任何基于这批报告的阅读/计分都不可信。

### 3) `update_report_section`（就地覆盖确认）

- `store.py` 的 `enqueue_block_job / latest_block_fingerprint / queued_block_jobs / finish_block_job / update_report_section` 均为**并行会话的未提交工作区改动**（git diff 可见，非本会话所加）。
- `update_report_section(report_id, section)` 是**就地 UPDATE** `company_cio_report_sections`（按 report_id+section_type 整节替换）——003012 id=286 的 PRICE 块附加字段（position_label/zone_low/zone_high/close_as_of）就是这样被 08-19 上下文的重算洗掉的。

### 4) 08-19 批统计（全表验证）

| 项 | 值 |
|---|---|
| research_as_of='2026-08-19' 行数 | **683** |
| BLOCK_ONLY_TEMPLATE（块刷新无存档路径） | **542** |
| LLM 全文（LLM_COMPLETED / LLM_PARTIAL） | **98 / 12** |
| TEMPLATE_FALLBACK / TEMPLATE | 30 / 1 |
| 最早 / 最晚 created_at | 16:07:24 / 16:08:18（北京） |
| 停止后是否还在涨 | **否**（核查至 16:2x 零增长） |

另有本会话下午合法写入的 09-11 块刷新行 1 条（003012 id=286，其中 valuation 节后被并行改动就地覆盖）。

### 5) 003012 现状

- 行 3 条：id=49（09-07 全文）、id=286（09-11 本会话 PRICE 块刷新）、**id=506（08-19 全文，16:07:39 由失控批写入）**。
- 最新 research_as_of=**2026-09-11**（id=286 仍为最新研究日）；valuation 现价 **4.57** 仍在，但 **position_label / zone_low / zone_high 已被就地覆盖为空**（id=506 的 08-19 全文另计）。

## 未删库

按指令**未 DELETE** 任何行。683 行 08-19 错基准日报告全部留存，等待处置指令（建议：DELETE research_as_of='2026-08-19' AND created_at>='2026-09-11T07:55' 的行 + 回填 003012 id=286 的 PRICE 块字段，二者都等授权）。

## 待用户决策

1. 删除 683 行错基准日报告（或保留为审计）。
2. 摘除/修复并行会话的批量触发面（block_worker 收敛 + `_default_research_as_of` fallback 在 TDX 未就绪时应拒绝生成而非落 08-19）。
3. backend 目前**停止状态**，何时重启由用户决定（重启前建议先处理上面两条）。
